---
description: Convert a markdown PRD to prd.json for autonomous execution
argument-hint: <path/to/prd.md>
---

Use the custom droid `prd-to-json` to convert the PRD at $ARGUMENTS to prd.json format.

Read the PRD file, convert each user story to JSON format with proper ordering and verifiable acceptance criteria, then save as prd.json.
