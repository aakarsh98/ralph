#═══════════════════════════════════════════════════════════════════════════════
# Ralph GUI Test Configuration (UI-TARS)
#
# Copy this file to config.env and fill in your values:
#   cp config.env.example config.env
#
# Then source it before running tests:
#   source config.env && ./run-gui-test.sh ./prd.json
#
# Or on Windows PowerShell:
#   . .\config.env.ps1; .\run-gui-test.ps1 .\prd.json
#═══════════════════════════════════════════════════════════════════════════════

#───────────────────────────────────────────────────────────────────────────────
# OPTION 1: Self-hosted UI-TARS (Free - requires your own GPU)
#───────────────────────────────────────────────────────────────────────────────
# Deploy UI-TARS-1.5-7B on your own infrastructure
# See: https://huggingface.co/ByteDance-Seed/UI-TARS-1.5-7B

export UI_TARS_PROVIDER="ui-tars"
export UI_TARS_MODEL="ui-tars-1.5-7b"
export UI_TARS_BASE_URL="http://localhost:8000/v1"  # Your local endpoint
export UI_TARS_API_KEY="your-local-api-key"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 2: VolcEngine / ByteDance Cloud (Recommended for ease of use)
#───────────────────────────────────────────────────────────────────────────────
# Sign up: https://console.volcengine.com/ark
# Model: Doubao-1.5-UI-TARS

# export UI_TARS_PROVIDER="volcengine"
# export VOLCENGINE_API_KEY="your-volcengine-api-key"
# export VOLCENGINE_BASE_URL="https://ark.cn-beijing.volces.com/api/v3"
# export VOLCENGINE_MODEL="doubao-1-5-ui-tars-250328"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 3: Hugging Face Inference Endpoints (Pay per use)
#───────────────────────────────────────────────────────────────────────────────
# Deploy: https://huggingface.co/ByteDance-Seed/UI-TARS-1.5-7B
# Create an Inference Endpoint and get your URL

# export UI_TARS_PROVIDER="huggingface"
# export HUGGINGFACE_API_KEY="hf_your_token_here"
# export HUGGINGFACE_BASE_URL="https://your-endpoint.endpoints.huggingface.cloud/v1/"
# export HUGGINGFACE_MODEL="ByteDance-Seed/UI-TARS-1.5-7B"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 4: Agent TARS CLI (Full browser automation)
#───────────────────────────────────────────────────────────────────────────────
# Uses Agent TARS for complete browser control
# Requires any vision model API (OpenAI, Anthropic, VolcEngine)

# export UI_TARS_PROVIDER="agent-tars"
# export VLM_API_KEY="your-api-key"
# export VLM_MODEL="doubao-1-5-thinking-vision-pro-250428"

#───────────────────────────────────────────────────────────────────────────────
# Generic Fallback (works with any OpenAI-compatible API)
#───────────────────────────────────────────────────────────────────────────────
# export VLM_API_KEY="your-api-key"
# export VLM_BASE_URL="https://api.openai.com/v1"
# export VLM_MODEL="gpt-4o"
# export VLM_PROVIDER="openai"
