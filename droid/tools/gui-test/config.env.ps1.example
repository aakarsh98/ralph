#═══════════════════════════════════════════════════════════════════════════════
# Ralph GUI Test Configuration (UI-TARS) - PowerShell Version
#
# Copy this file to config.env.ps1 and fill in your values:
#   Copy-Item config.env.ps1.example config.env.ps1
#
# Then dot-source it before running tests:
#   . .\config.env.ps1; .\run-gui-test.ps1 .\prd.json
#═══════════════════════════════════════════════════════════════════════════════

#───────────────────────────────────────────────────────────────────────────────
# OPTION 1: Self-hosted UI-TARS (Free - requires your own GPU)
#───────────────────────────────────────────────────────────────────────────────
$env:UI_TARS_PROVIDER = "ui-tars"
$env:UI_TARS_MODEL = "ui-tars-1.5-7b"
$env:UI_TARS_BASE_URL = "http://localhost:8000/v1"  # Your local endpoint
$env:UI_TARS_API_KEY = "your-local-api-key"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 2: VolcEngine / ByteDance Cloud (Recommended for ease of use)
#───────────────────────────────────────────────────────────────────────────────
# $env:UI_TARS_PROVIDER = "volcengine"
# $env:VOLCENGINE_API_KEY = "your-volcengine-api-key"
# $env:VOLCENGINE_BASE_URL = "https://ark.cn-beijing.volces.com/api/v3"
# $env:VOLCENGINE_MODEL = "doubao-1-5-ui-tars-250328"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 3: Hugging Face Inference Endpoints (Pay per use)
#───────────────────────────────────────────────────────────────────────────────
# $env:UI_TARS_PROVIDER = "huggingface"
# $env:HUGGINGFACE_API_KEY = "hf_your_token_here"
# $env:HUGGINGFACE_BASE_URL = "https://your-endpoint.endpoints.huggingface.cloud/v1/"
# $env:HUGGINGFACE_MODEL = "ByteDance-Seed/UI-TARS-1.5-7B"

#───────────────────────────────────────────────────────────────────────────────
# OPTION 4: Agent TARS CLI (Full browser automation)
#───────────────────────────────────────────────────────────────────────────────
# $env:UI_TARS_PROVIDER = "agent-tars"
# $env:VLM_API_KEY = "your-api-key"
# $env:VLM_MODEL = "doubao-1-5-thinking-vision-pro-250428"
